import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    background: {
      paper: "#1e1e1e",
    },
    secondary: { main: "#73cff" },
    text: {
      primary: "#ffffff",
      secondary: "#cccccc",
    },
  },
});
